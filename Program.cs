using System;
using Word_prefix.Calculator;

namespace Word_prefix
{
    class Program
    {
        static void Main(string[] args)
        {
            var fileUrl = @"";
            WordCalculator.Run(fileUrl);
        }
    }
}
